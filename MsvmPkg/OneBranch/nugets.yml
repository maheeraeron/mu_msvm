# download multiple Nuget packages

parameters:
- name: packages
  type: object

steps:
- ${{ each package in parameters.packages }}:
  - template: nuget.yml@self
    parameters:
      name:    ${{package.name}}
      version: ${{package.version}}
