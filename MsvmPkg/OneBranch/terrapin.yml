# Download one package with Terrapin.

parameters:
- name: name
- name: url
- name: hash
- name: path

steps:
- script: MsvmPkg\OneBranch\terrapin.cmd ${{parameters.url}} ${{parameters.hash}} ${{parameters.path}}
  displayName: terrapin ${{parameters.name}} to Stuart ExtDep cache
